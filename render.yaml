services:
  - type: web
    name: anitoki-backend
    env: python
    branch: main
    buildCommand: bash scripts/render_build.sh
    startCommand: bash scripts/start_debug.sh
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: core.settings
    healthCheckPath: /api/health/
